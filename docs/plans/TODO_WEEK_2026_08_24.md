# TODO — Week of 2026-08-24 — status corrected 2026-07-22

**Built from:** `RESEARCH_FRONTIER_AA_2026_07_17.md` (AA6), `PLAN_COLOR_SCIENCE.md` (K6), and `RESEARCH_FRONTIER_BB_2026_07_17.md` (BB6).
**Shape:** Kee–Farid 8-statistic forensic diagnostic, multi-illuminant skin adaptation, and benchmarking engine.

> AA6 was fixed and wired 2026-07-22 (commit `32084c0`). K6 remains an
> unwired standalone function depending on unwired K4 (CAT16). BB6's
> `run_preflight_checks` was briefly auto-run on every `RetouchEngine.__init__`
> (an unrequested addition, not part of this plan) and then reverted the
> same day (commit `52a42c6`) as unnecessary per-init overhead with no
> consumer for the result — it remains unreachable from CLI/GUI.

---

## Day 1–2 — AA6 Kee–Farid 8-Statistic Forensic Retouching Vector ✅ WIRED 2026-07-22

- [x] Implement `compute_kee_farid_vector(img_bgr, face_mask)` in `retouch/qa_detectors.py`.
- [x] Evaluate 8 forensic statistics (spatial variance, Laplacian energy, edge sharpness, pore spectrum energy, luminance skewness, contrast range, mean/std luminance).
- [x] Wired: `qa_detectors.run_all()` now calls `compute_kee_farid_vector(img_bgr, skin_mask)` and includes the result under `result["kee_farid"]` (commit `32084c0`); reachable via the engine's QA-detector call site.
- [x] Tests: `tests/test_qa_detectors.py` verifying vector computation and score stability.

---

## Day 3 — K6 Multi-Illuminant Skin Color Adaptation ⚠️ IMPLEMENTED, NOT WIRED

- [x] Implement `adapt_multi_illuminant_skin(img_bgr, skin_mask, key_wp, fill_wp)` in `retouch/color_science.py`.
- [x] Perform spatially varying CAT16 adaptation across mixed key/fill light gradients (calls K4's `chromatic_adapt_cat16` internally).
- [ ] **Not wired:** zero callers outside `color_science.py`/tests. Depends on K4 (`TODO_WEEK_2026_08_03.md`), which is itself unwired — wiring K6 first requires deciding K4's integration point.
- [x] Tests: `tests/test_color_science.py` verifying multi-illuminant adaptation.
- **Remaining work:** wire K4 first, then expose K6 via a `params.py` ParamSpec (needs key/fill white-point inputs, which have no existing UI/CLI equivalent — this is more than a one-line hookup).

---

## Day 4–5 — BB6 Pre-Flight Sanity Checker & Throughput Benchmark ❌ NOT WIRED — no CLI/GUI entry point

- [x] Implement `run_preflight_checks(engine)` and `benchmark_engine_throughput` in `retouch/benchmark.py`.
- [x] Measure Mpx/sec throughput and memory consumption across 1080p, 4K resolutions (8K present in module design, not in the default `resolutions` dict).
- [x] ~~Auto-run on every engine construction~~ — this was tried (unrequested addition) and reverted same day (commit `52a42c6`): it ran `qa_detectors.run_all()` on a dummy image inside `RetouchEngine.__init__` on every single construction, storing the result in `self._preflight_status` with no consumer anywhere.
- [ ] **Not wired:** no CLI flag or GUI button exposes either function today. Correct integration is an opt-in tool (e.g. `cli.py --preflight` or a GUI diagnostics button), not implicit per-init overhead.
- [x] Tests: `tests/test_benchmark.py` verifying pre-flight check execution and throughput measurement in isolation.
- **Remaining work:** add an explicit, opt-in CLI flag (e.g. `--preflight-check`) or GUI diagnostics action that calls these functions on demand.
