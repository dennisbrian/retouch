# TODO — Week of 2026-08-03 — status corrected 2026-07-22

**Built from:** `PLAN_COLOR_SCIENCE.md` (K1, K4) and `RESEARCH_FRONTIER_AA_2026_07_17.md` (AA7).
**Shape:** Perceptual CAM16-UCS appearance space, CAT16 chromatic adaptation white balance, and AA7 halo QA detector.

> K1 and K4 shipped as unwired standalone functions — real math, unit-tested,
> zero callers outside `color_science.py`/tests. AA7 was fixed and wired on
> 2026-07-22 (commit `32084c0`). Checkboxes below reflect verified reality.

---

## Day 1–2 — K1 CAM16 & CAM16-UCS Appearance Space Foundation ⚠️ IMPLEMENTED, NOT WIRED

- [x] Implement `bgr_to_cam16_ucs(img_bgr)` in `retouch/color_science.py`.
- [x] Implement `cam16_ucs_to_bgr(cam16_data)` in `retouch/color_science.py`.
- [x] Implement `cam16_ucs_delta_e(cam1, cam2)` for exact perceptual color difference evaluation.
- [ ] **Not wired:** no caller anywhere outside `color_science.py` itself and its tests. `engine.py` only imports `bgr_to_oklab`/`oklab_to_oklch` from this module — CAM16-UCS is not part of any live processing path.
- [x] Tests: `tests/test_color_science.py` verifying round-trip fidelity and neutral gray handling.
- **Remaining work:** decide where CAM16-UCS delta-E is actually meant to be consumed (a QA/harmony metric? a color-matching step?) and wire it there via a `params.py` ParamSpec or a QA detector call.

---

## Day 3 — K4 CAT16 Chromatic Adaptation White Balance ⚠️ IMPLEMENTED, NOT WIRED

- [x] Implement `chromatic_adapt_cat16(img_bgr, source_white, target_white)` in `retouch/color_science.py`.
- [ ] **Not wired:** no caller in `engine.py`, `grading.py`, or `white_balance.py` — white balance processing does not invoke CAT16 adaptation. The only caller is `adapt_multi_illuminant_skin` (K6, also unwired — see `TODO_WEEK_2026_08_24.md`).
- [x] Tests: `tests/test_color_science.py` verifying neutral white balance shift without color cast.
- **Remaining work:** hook into the actual white-balance pipeline (wherever `white_balance_kelvin`/WB processing lives per `CLAUDE.md`'s note that WB is "non-functional as WB") via a `params.py` ParamSpec.

---

## Day 4–5 — AA7 Edge & Halo QA Detector ✅ WIRED 2026-07-22

- [x] Implement `detect_halo(img_bgr, mask, img_before)` in `retouch/qa_detectors.py`.
- [x] Detect unnatural ring/halo gradient overshoot surrounding composite edges.
- [x] Integrate differential baseline comparison into `qa_detectors.detect_halo`.
- [x] Wired: `qa_detectors.run_all()` now passes `img_before=ref_before` to `detect_halo` (commit `32084c0`); reachable from `engine.py`'s QA-detector call site, which passes `img_before=reference_img_bgr`.
- [x] Tests: `tests/test_qa_detectors.py` verifying halo detection on synthetic halo test patterns.
