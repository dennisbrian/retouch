# TODO — Week of 2026-08-31 — status corrected 2026-07-22

**Built from:** `RESEARCH_FRONTIER_AA_2026_07_17.md` (AA8), `PLAN_COLOR_SCIENCE.md` (K7), and `RESEARCH_FRONTIER_BB_2026_07_17.md` (BB7).
**Shape:** Micro-texture grain synthesis, HDR10 PQ transfer curve, and multi-threaded batch queue engine.

> AA8 and K7 shipped as unwired dead code — AA8 in particular is a dead
> duplicate of already-wired, pre-existing grain logic. BB7 is the one item
> in this whole 7-week set that's genuinely real and wired, though it took
> two attempts to get the return-type contract right.

---

## Day 1–2 — AA8 Micro-Texture Grain Synthesis & Detail Retention ❌ DEAD DUPLICATE — not wired

- [x] Implement `synthesize_micro_grain(img_bgr, skin_mask, intensity)` in `retouch/texture.py`.
- [x] Tone-adaptive noise injection matching skin micro-pore statistics.
- [~] Wired into `params.py`/`engine.py` on 2026-07-22 (ParamSpec `micro_grain`, CLI `--micro-grain`, `engine.py::_stage_finish` gated on `ctx.micro_grain > 0`, commit `32084c0`) — **but** the pre-existing, already-wired `grain.py::apply_film_grain` (called from `engine.py` at 3 separate sites for film-grain/impact-finish) does the same conceptual job and is what recipes/GUI actually drive today. `micro_grain` is a new, additive, independent control, not a replacement — worth confirming with product intent whether these two grain systems should be merged or genuinely kept separate.
- [x] Tests: `tests/test_texture.py` verifying grain synthesis.

---

## Day 3 — K7 HDR10 PQ (ST 2084) Perceptual Transfer Functions ⚠️ IMPLEMENTED, NOT WIRED

- [x] Implement `linear_to_pq(img_linear)` and `pq_to_linear(img_pq)` in `retouch/color_science.py`.
- [x] Support ST 2084 Perceptual Quantizer EOTF/OETF math.
- [ ] **Not wired:** zero callers outside `color_science.py`/tests. No HDR/EXR export path invokes it — consistent with BB3 (EXR I/O) also being unimplemented, since PQ transfer only matters once there's an actual HDR export target to feed it.
- [x] Tests: `tests/test_color_science.py` verifying PQ transfer function round-trip.
- **Remaining work:** blocked on BB3 (`TODO_WEEK_2026_08_10.md`) landing a real HDR export path first; PQ encode/decode has nowhere to plug in until then.

---

## Day 4–5 — BB7 Multi-Threaded Async Batch Processor Queue ✅ WIRED 2026-07-22 (two iterations)

- [x] Enhance `BatchProcessor` in `retouch/batch_processor.py` with concurrent worker dispatch.
  - First iteration (commit `3c50fef`): `ThreadPoolExecutor`-based dispatch via a new `num_workers` param and extracted `_process_single_file`. Verified live with a real 6-image concurrent render through `RetouchEngine` — correct, no crashes, no output corruption.
  - Second iteration (commit `ae990b5`): replaced with a bounded producer-consumer queue (`_run_async_batch_queue`) that caps in-flight images via `_compute_queue_depth`, sampling image sizes to bound RAM usage (default 2 GB budget) instead of firing all tasks into the pool at once — this is what "RAM pre-allocation" below actually refers to.
- [x] Optimize 4K/8K batch throughput and RAM pre-allocation: `_compute_queue_depth` bounds queue depth from sampled image byte-size estimates against `_DEFAULT_RAM_BUDGET_BYTES`.
- [x] **Bug caught and fixed:** the queue rewrite initially returned `List[str]` while the sequential fallback path returned `List[Path]`; downstream ZIP packaging (`exp_path.name`) and contact-sheet generation assumed `Path`, so `export_zip=True` runs crashed with `'str' object has no attribute 'name'`. Fixed by making both paths consistently return `List[Path]` (commit `ae990b5`); two unit tests that had been written against the buggy `str` contract were corrected to assert on `Path`.
- [x] Tests: `tests/test_batch_processor.py` (20 passed) + `tests/test_batch_workflow.py`, plus a live end-to-end run with `num_workers=3, export_zip=True, generate_sheet=True` on 5 real images — ZIP contents verified to contain all 5 retouched files plus the contact sheet.
