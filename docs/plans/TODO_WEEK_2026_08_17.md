# TODO — Week of 2026-08-17 — status corrected 2026-07-22

**Built from:** `RESEARCH_FRONTIER_AB_2026_07_17.md` (AA5), `PLAN_COLOR_SCIENCE.md` (K5), and `RESEARCH_FRONTIER_BB_2026_07_17.md` (BB5).
**Shape:** Corneal curvature realism, Wide-Color-Gamut P3/Rec.2020 mapping, and GPU acceleration fallback.

> AA5 and K5 shipped as unwired standalone functions. BB5's acceleration
> layer is genuinely wired, but into exactly one caller (`feather_mask`) —
> functionally a no-op on any machine without `torch` installed (the case
> on this dev machine), so its GPU path has not actually been exercised yet.

---

## Day 1–2 — AA5 Dynamic Cornea Specular Curvature Realism ⚠️ IMPLEMENTED, NOT WIRED

- [x] Implement `apply_corneal_curvature_shading(eye_crop, iris_mask, strength)` in `retouch/eyes.py`.
- [x] Model 3D spherical normal mapping over eyeball geometry to boost corneal wetness and 3D depth.
- [ ] **Not wired:** `EyeEnhancer.enhance()` never calls this function — grep confirms the only match for `apply_corneal_curvature_shading` in `retouch/eyes.py` is the definition itself.
- [x] Tests: `tests/test_eyes.py` verifying 3D corneal shading and highlight placement (exercises the function directly, not through `enhance()`).
- **Remaining work:** call it from `EyeEnhancer.enhance()` gated on a new/existing strength param, and confirm it composes correctly with the already-wired iris sculpting and catchlight steps (AA4/BB1) rather than fighting them for the same iris crop region.

---

## Day 3 — K5 Wide-Color-Gamut (Display P3 / Rec.2020) Gamut Mapping ⚠️ IMPLEMENTED, NOT WIRED

- [x] Implement `find_gamut_intersection_p3(L, h)` and `find_gamut_intersection_rec2020(L, h)` in `retouch/color_science.py`.
- [x] Support soft-knee chroma compression targeted for P3 and Rec.2020 color spaces.
- [ ] **Not wired:** only `find_gamut_intersection_srgb` (via `compress_chroma_gamut`, K3) is actually called from `engine.py::_stage_finish`. The P3/Rec.2020 variants have no caller — the wired gamut-compression stage is hardcoded to sRGB.
- [x] Tests: `tests/test_color_science.py` verifying P3 / Rec.2020 gamut boundary calculations.
- **Remaining work:** decide how a P3/Rec.2020 target gamut gets selected at runtime (export format? a new param?) and thread that choice into the `_stage_finish` gamut-compression call instead of the hardcoded sRGB path.

---

## Day 4–5 — BB5 GPU Acceleration & Vectorized Batch Operations ✅ WIRED (narrow scope, untested on real GPU)

- [x] Implement optional CUDA/Metal acceleration layer in `retouch/acceleration.py`.
- [x] Provide pure NumPy/OpenCV CPU fallback when PyTorch is unavailable.
- [x] Wired: `retouch/utils.py::feather_mask` calls `accelerated_gaussian_blur` instead of `cv2.GaussianBlur` directly. This is the **only** call site in the engine's live pipeline; `benchmark.py::run_preflight_checks` also references `get_acceleration_backend` but that preflight function itself is unwired (see `TODO_WEEK_2026_08_24.md`, BB6).
- [x] Tests: `tests/test_acceleration.py` verifying CPU vs. accelerated path equivalence.
- **Caveat:** verified as a correctness no-op only — this dev machine has no `torch` installed, so every call falls through to the identical `cv2.GaussianBlur`. The actual GPU/MPS `conv2d` code path has never been exercised or checked for numerical parity against the CPU path (border-handling differences between `torch.nn.functional.conv2d` and `cv2.GaussianBlur` are a known risk, per today's audit).
