# TODO — Week of 2026-07-27 — status corrected 2026-07-22

**Built from:** `RESEARCH_FRONTIER_AB_2026_07_17.md` (AB3, AB4), `RESEARCH_FRONTIER_BB_2026_07_17.md` (BB2), and `PLAN_COLOR_SCIENCE.md` (K3).
**Shape:** Color fidelity, edge-defect restoration, metadata provenance, and stray hair cleanup.

> Every item below originally shipped as an unwired standalone function —
> implemented and unit-tested in isolation, but not reachable from
> `RetouchEngine.process()`, `params.py`, `cli.py`, or `gui.py`. An audit on
> 2026-07-22 caught this; K3/AB4/AB3 were then actually wired (commit
> `32084c0`). BB2 remains partial. Checkboxes below reflect verified
> reality, not the original claim.

---

## Day 1 — K3 Gamut-Aware Soft-Knee Chroma Compression ✅ WIRED 2026-07-22

- [x] Implement `find_gamut_intersection_srgb(L, h)` in `retouch/color_science.py` using Ottosson's OKLCh sRGB boundary solver.
- [x] Implement soft-knee chroma compression `compress_chroma_gamut(oklch, knee=0.85)` in `color_science.py`.
- [x] Hook gamut compression into the live pipeline — wired unconditionally at the end of `engine.py::_stage_finish` (commit `32084c0`), not into `skin.py`/`grading.py` as originally planned.
- [x] Tests: `tests/test_color_science.py` verifying saturation sweeps stay inside sRGB gamut without channel clipping or hue shift.

---

## Day 2 — AB4 Purple Fringing & Chromatic Aberration Removal ✅ WIRED 2026-07-22

- [x] Implement `remove_purple_fringing(img_bgr, threshold)` in `retouch/utils.py`.
- [x] Detect fringe pixels at high-luminance gradient edges adjacent to blue-violet chroma spikes.
- [x] Replace fringe chroma with directional interpolation from neighboring non-edge pixels.
- [x] Gated strictly to luminance edge boundaries so purple hair/costumes/irises are preserved.
- [x] Wired: `params.py` ParamSpec `purple_fringing` (CLI `--purple-fringing`, recipe key `finish.purple_fringing`) → `engine.py::_stage_finish`, gated on `ctx.purple_fringing > 0` (commit `32084c0`). GUI: `gr.State` placeholder only, no slider yet.
- [x] Tests: `tests/test_utils.py` verifying fringe desaturation on high-contrast edge test patterns.

---

## Day 3 — BB2 C2PA / Content Credentials Provenance Preservation ⚠️ PARTIAL — reader only

- [x] Read C2PA manifest JUMBF metadata headers: `retouch/io.py::read_c2pa_manifest`.
- [ ] **Not implemented:** no writer/embedder exists anywhere in the codebase. "Preservation" implies re-embedding the manifest into the retouched export; `write_image_with_icc` does not touch C2PA at all.
- [ ] **Not wired:** no caller in `cli.py` or `batch_processor.py` reads or threads a manifest through the export path.
- [x] Tests: `tests/test_io_icc.py` exercises the reader only.
- **Remaining work:** implement a C2PA manifest writer/embedder and thread it through `write_image_with_icc` (or a dedicated export step), then call it from `cli.py`/`batch_processor.py`.

---

## Day 4–5 — AB3 Flyaway-Strand Cleanup ✅ WIRED 2026-07-22

- [x] Implement curvilinear ridge detection in `retouch/hair.py` (`cleanup_flyaway_strands`).
- [x] Restrict detection to skin/background region outside hair mass silhouette.
- [x] Inpaint detected stray strands using Telea inpainting.
- [x] Wired: `params.py` ParamSpec `flyaway_cleanup` (CLI `--flyaway-cleanup`, recipe key `hair.flyaway_cleanup`) → `engine.py`, gated on `ctx.flyaway_cleanup > 0`, runs before Stage 4 global (commit `32084c0`). Note: this is a **separate, differently-named feature** from the pre-existing `hair_remove_flyaways` / `remove_flyaways` (in `retouch/hairwork.py`), which was already wired before this branch and remains the default flyaway path in recipes/GUI.
- [x] Tests: `tests/test_hair.py` verifying strand removal on synthetic test crops.
