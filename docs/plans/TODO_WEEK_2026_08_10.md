# TODO — Week of 2026-08-10 — status corrected 2026-07-22

**Built from:** `RESEARCH_FRONTIER_AB_2026_07_17.md` (AA4), `RESEARCH_FRONTIER_BB_2026_07_17.md` (BB3, BB4).
**Shape:** Catchlight synthesis, 32-bit EXR float I/O, and OKLCh split toning.

> AA4 was already reachable but the flag that activates its new code path was
> never set — fixed 2026-07-22 (commit `32084c0`). BB4 shipped as an unwired
> dead duplicate of already-wired split-toning code and was wired the same
> day, but into a different call site than originally planned. BB3 is a
> stub — extension-string registration only, no actual EXR codec.

---

## Day 1–2 — AA4 Iris Pattern-Preserving Catchlight Synthesis ✅ WIRED 2026-07-22

- [x] Implement `enhance_catchlights` (aka `_enhance_catchlights`) in `retouch/eyes.py`.
- [x] Specular highlight enhancement preserving underlying iris radial fibril patterns.
- [x] Hard confidence gate for dark irises ($L^* < 30.0$) and upper-quadrant synthetic fallback.
- [x] Wired: the method was already called from `EyeEnhancer.enhance()` on the hot path, but `synthetic_fallback=True` was never passed — the new fallback code was dead. Fixed by passing it explicitly at the call site (commit `32084c0`).
- [x] Tests: `tests/test_eyes.py` verifying catchlight specular boost without iris pattern destruction.

---

## Day 3 — BB3 High-Bit-Depth EXR 32-bit Float I/O ❌ STUB — no codec implemented

- [ ] **Not implemented:** `".exr"` was added to `IMAGE_EXTENSIONS`/`EXT_MAP` dicts in `retouch/io.py` only. No `imread`/`imwrite` path actually handles `.exr` — `read_image_16bit` only special-cases `.png`/`.tif`/`.tiff` for `IMREAD_UNCHANGED`. There is no float32 EXR reader or writer function anywhere in the codebase.
- [ ] Float32 uncompressed linear buffer support: not present.
- [x] Tests: `tests/test_io_icc.py` exercises only the extension-map entries, not actual EXR encode/decode (there's nothing to test).
- **Remaining work:** this needs an actual EXR library dependency (e.g. `OpenEXR`/`Imath` or `imageio`'s EXR plugin) and real read/write functions before any of the above can be checked off.

---

## Day 4–5 — BB4 OKLCh Dual-Tone Split Toning ✅ WIRED 2026-07-22 (different call site than planned)

- [x] Implement `apply_split_toning(img_bgr, shadow_hue, highlight_hue, balance)` in `retouch/grading.py`.
- [x] Perceptually uniform shadow vs. highlight hue separation in OKLCh.
- [x] Wired: `params.py` ParamSpec `split_toning` (CLI `--split-toning`, recipe key `finish.split_toning`) → `engine.py::_stage_finish`, gated on `ctx.split_toning > 0` (commit `32084c0`). Note: a **separate, pre-existing** split-toning system (`shadow_hue`/`shadow_sat`/`midtone_hue`/... via `_SPLIT_TONING_PARAMS`, `grading.py::_split_tone_three_way`/`split_tone_lch`) was already wired into GUI/recipes before this branch and remains the one driving the visible split-toning sliders — this new `split_toning` param is an additive, independent control, not a replacement.
- [x] Tests: `tests/test_grading.py` verifying split-toning hue shift.
