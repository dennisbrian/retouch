opencv-contrib-python>=4.8.0,<4.12
# 0.10.5 is the tested macOS legacy CPU/XNNPACK graph. Later legacy wheels
# still request NSOpenGLPixelFormat on this host, while 0.10.31+ removed the
# Solutions API and 0.10.35 can abort in the Tasks graph.
mediapipe==0.10.5
# MediaPipe 0.10.5 requires the pre-4 protobuf API.
protobuf>=3.20,<4
numpy>=1.24.0,<2
onnxruntime>=1.16.0
tqdm>=4.65.0
Pillow>=10.0.0
jsonschema>=4.18.0,<5
