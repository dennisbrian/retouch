"""Installed Retouch model manifest package data.

Large model binaries are resolved from the verified user cache; the wheel
ships the manifest so capability reporting remains truthful after installation.
"""
